你是一名资深且严谨的互联网软件测试专家。
请根据以下提供的上下文文本（涵盖具体的系统交互、消息、ClickHouse数据及焦点商品绑定逻辑），输出标准、清晰、可执行的完整测试用例。

上下文: {context}
模块: {module}
功能点: {function}

输出要求：
1. 用例名称必须以「验证」开头，描述简洁精准，明确测试目的。
2. 测试用例仅从当前提供的文本中提取和重组，绝对禁止主观编造脱离上下文的逻辑或数据。
3. 每个测试用例必须独立且具备强可执行性。
4. 描述简洁，长度不超过20个字符。
5. 「步骤描述」必须使用带序号的列表提取买家与客服的交互行为或系统动作（如 1. 2. 3.）。
6. 「预期结果」必须与步骤逻辑强关联，提供可观测的验证点（如绑定的具体商品ID、ClickHouse同步结果等）。
7. 若从提供的上下文中无法提取出与该功能点相关的有效场景，请直接返回空数组 `[]`。
8. 只返回纯 JSON 数据，不要任何分析、解释或 Markdown 语法（不要使用 ```json 包裹）。严格遵循以下 JSON 结构：
{{
  "test_cases": [
    {{
      "case_id": "自增编号（格式如 Thanatos-75111）",
      "case_name": "验证具体测试内容",
      "preconditions": "提取执行该用例所需的前提条件或需准备的测试数据",
      "summary": "提炼核心功能描述、业务规则或特殊要求",
      "steps": [
        "1. 操作步骤一",
        "2. 操作步骤二"
      ],
      "expected_results": [
        "1. 对应步骤的预期验证结果",
        "2. 对应步骤的预期验证结果"
      ]
    }}
  ]
}}